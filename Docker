<<<<<<< HEAD
-- Windows
//Acesse o site abaixo e faço o download do software
https://docs.docker.com/desktop/install/windows-install/

//Para add o docker no seu projeto para na raiz do mesmo você criar os seguintes arquivos: Dockerfile e docker-compose.yml.

//Adicionando os seguintes codígos:
-- Dockerfile
# Use a imagem oficial do PHP como base
FROM php:7.4-fpm

# Diretório de trabalho dentro do contêiner
WORKDIR /var/www/html

# Instale as extensões do PHP necessárias
RUN docker-php-ext-install pdo pdo_mysql

# Copie os arquivos do projeto Laravel para o contêiner
COPY . .

# Instale as dependências do Composer
RUN curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
RUN composer install

# Exponha a porta 9000 para o servidor web
EXPOSE 9000

# Comando para iniciar o servidor PHP
CMD ["php-fpm"]

----------------------------------------------------------------
-- docker-compose.yml

version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel-app
    ports:
      - "8000:9000"
    volumes:
      - .:/var/www/html

-----------------------------------------------------------------

//Abra um terminal, navegue até o diretório do seu projeto Laravel e execute os seguintes comandos:
docker-compose build   # Isso construirá a imagem do Docker
docker-compose up      # Isso iniciará os contêineres

composer install
=======
teste
>>>>>>> 24ae72613ae5c39a117caafa59befcd104f8edbc
